function add(a: number, b: number): number {
  return a + b;
}

let result = add(10, 20); // Correct

function addString(a: string, b: string): string {
  return a + b; 
}

let stringResult = addString("10", "20"); // Correct string addition